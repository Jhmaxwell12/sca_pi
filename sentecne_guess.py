#!/usr/bin/env python
from sense_hat import SenseHat
sense = SenseHat()
import time

speed=0
s = "Hello World!" 
r=(255, 255, 255)
while i < 1
    speed = speed + .05
    sense.show_message(s, speed, text_colour=r)
    







sense.clear()
